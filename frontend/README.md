# Frontend for TODO List App

This app is now using Vite since the original version from the awesome-compose repo used create-react-app, which
is no longer maintained as of 2025.

See `package.json` for the run commands.
